APP_ENV=development
APP_NAME=Cerebrum
LOG_LEVEL=INFO
CEREBRUM_VAULT_PATH=../vault
CORS_ORIGINS=["http://localhost:5173"]

# Required; no default. Generate each with: openssl rand -hex 32
AUTH_JWT_SECRET=
AUTH_SETUP_TOKEN=
