"""Standalone maintenance/build-time scripts for cerebrum.

Distinct from `api/`, `notes/`, `index/`, etc. -- everything under this
package is a console-script entry point invoked directly (see
`pyproject.toml`'s `[project.scripts]`), never imported by the running
application itself.
"""
